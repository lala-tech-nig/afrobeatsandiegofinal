import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "AfroBeat San Diego",
  description: "Afrobeat San Diego is your ultimate hub for Afrobeat culture, entertainment, and news. Stay updated daily with the latest in Afrobeats, exclusive celebrity stories, and rich African cultural content. We bring Africa closer to you — from music trends to lifestyle, fashion, and more — all from the heart of San Diego.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
